# 📘 คู่มือ Nuxt 3 + TypeScript + Pinia (แบบเข้าใจง่าย ไม่งง Auto-Import)

คู่มือนี้เขียนจาก **เคสใช้งานจริง** ที่เจอปัญหา TS แดง, import เพี้ยน, VS Code งง โดยเฉพาะตอนทำงานกับ **Docker + Dev Container**

เหมาะสำหรับ:

* คนใช้ Nuxt 3 / Nuxt 3+
* ใช้ TypeScript
* ใช้ Pinia
* เปิดโปรเจกต์ผ่าน VS Code + Dev Container

---

## 1️⃣ เริ่มต้นให้ถูก: เปิด VS Code จาก Terminal

> ❗ สำคัญมาก ถ้าเปิดผิดตั้งแต่ต้น TS จะงงทั้งโปรเจกต์

### ✅ วิธีที่ถูกต้อง

```bash
cd ~/Desktop/Project/CRM
code .
```

แล้วใน VS Code:

1. กดปุ่ม ctrl+shift+p 
2. พิมพ์ **“Reopen in Container”**
3. รอให้ Container build เสร็จ
4. ดูมุมซ้ายล่าง ต้องขึ้น:

```
Dev Container: nuxt-crm
```

ถ้าไม่ขึ้น = ❌ ยังไม่อยู่ในโลกของ Nuxt จริง

---

## 2️⃣ เช็กโครงสร้างโปรเจกต์ (ตัวอย่างที่ถูก)

```
CRM/
├─ app/
│  ├─ pages/
│  ├─ components/
│  ├─ layouts/
│  ├─ composables/
│  ├─ stores/
│  ├─ utils/
│  └─ app.vue
├─ node_modules/
├─ nuxt.config.ts
├─ tsconfig.json
├─ package.json
└─ docker-compose.yml
```

> 🔑 ทุกอย่างที่ Nuxt auto-magic จะอิงจากโฟลเดอร์ `app/`

---

## 3️⃣ โลกของ Nuxt มี 2 ชั้น (สำคัญมาก!)

### 🌍 ชั้นที่ 1: Nuxt คุมให้เต็มที่ (Auto-Import)

ไฟล์ประเภทนี้:

* `app/pages/*.vue`
* `app/components/*.vue`
* `app/layouts/*.vue`
* `<script setup>` ใน `.vue`

ในไฟล์พวกนี้:

```ts
const count = ref(0)
const state = reactive({ a: 1 })
```

✅ **ไม่ต้อง import**

```ts
// ❌ ไม่จำเป็น
import { ref, reactive } from 'vue'
```

Nuxt inject ให้เองอัตโนมัติ

---

### 🧱 ชั้นที่ 2: Nuxt ไม่ auto-import ให้ (ไฟล์ logic ล้วน)

ไฟล์พวกนี้:

* `app/stores/*.ts`
* `app/utils/*.ts`
* `app/db/*.ts`
* `app/services/*.ts`

ถึงจะอยู่ใน `app/` แต่:

* ❌ ไม่มี `<script setup>`
* ❌ ไม่ auto-import
* ❌ Nuxt ไม่ inject context

> 📌 **อยู่ในโปรเจกต์ Nuxt แต่ยังเป็น TypeScript ธรรมดา**

---

## 4️⃣ Pinia ต้อง import ไหม? (คำตอบชัด ๆ)

### ✅ ต้อง import เสมอ ในไฟล์ store

ตัวอย่างที่ถูกต้อง:

```ts
// app/stores/testStore.ts
import { defineStore } from 'pinia'

export const useTestStore = defineStore('testStore', {
  state: () => ({
    testStatus: 'พร้อมใช้งาน' as string,
  }),
  actions: {
    setTestStatus(val: string) {
      this.testStatus = val
    },
  },
})
```

เหตุผล:

* `defineStore` เป็นของ Pinia
* Nuxt ไม่ auto-import API ของ Pinia ให้ไฟล์ `.ts`

---

## 5️⃣ แล้วใช้ Pinia ในไฟล์ `.vue` ล่ะ?

```vue
<script setup lang="ts">
const testStore = useTestStore()
</script>
```

* ✅ **ไม่ต้อง import `useTestStore`**
* เพราะ Nuxt auto-import store จาก `app/stores`

---

## 6️⃣ สรุปจำง่ายใน 10 วินาที 🧠

| ตำแหน่งไฟล์           | ref / reactive   | defineStore      |
| --------------------- | ---------------- | ---------------- |
| `.vue` (script setup) | ❌ ไม่ต้อง import | ❌ ไม่ต้อง import |
| `stores/*.ts`         | (ไม่ควรใช้)      | ✅ ต้อง import    |
| `utils/*.ts`          | ❌ auto           | ❌ auto           |

> 🧠 **ดูที่นามสกุลไฟล์ ไม่ใช่ดูว่าอยู่ใน app หรือไม่**

---

## 7️⃣ กฎเหล็กกัน TS แดง (เอาไปใช้ได้จริง)

1. เปิด VS Code ด้วย `code .` เท่านั้น
2. ต้องเข้า Dev Container ก่อนเขียนโค้ด
3. `.vue` = Nuxt magic
4. `.ts` = TypeScript ธรรมดา
5. Pinia ต้อง import ใน store เสมอ

---

## 8️⃣ ถ้า TS ยังแดง ให้เช็กสิ่งนี้

* เปิด VS Code นอก container หรือไม่
* `node_modules` อยู่ใน container จริงไหม
* VS Code ใช้ TypeScript version จาก workspace หรือไม่
* ติดตั้ง extension ซ้ำซ้อน (Prettier / TS) หรือเปล่า

---

📌 **คู่มือนี้เขียนจากเคสจริง ไม่ใช่ทฤษฎีลอย ๆ**

ถ้าคุณอยากให้:

* ทำ version สำหรับทีม
* แยกเป็น onboarding dev ใหม่
* หรือเพิ่ม troubleshooting checklist แบบลึก

บอกได้เลย เดี๋ยวจัดให้ต่อครับ 👌
