# 📘 Git คู่มือการใช้งาน (ฉบับละเอียด ภาษาไทย)

> 📌 เอกสารนี้รองรับ **การกระโดดไปยังหัวข้อต่าง ๆ (Anchor Links)** เหมาะสำหรับอ่านอ้างอิงเร็วระหว่างทำงาน

---

## 🧭 สารบัญ (Table of Contents)

* [Git คืออะไร](#-git-คืออะไร)
* [โครงสร้างพื้นฐานของ Git](#-โครงสร้างพื้นฐานของ-git)
* [การตั้งค่า Git ครั้งแรก](#️-การตั้งค่า-git-ครั้งแรก)
* [เริ่มต้นโปรเจกต์ Git](#-เริ่มต้นโปรเจกต์-git)
* [ตรวจสอบสถานะไฟล์](#-ตรวจสอบสถานะไฟล์)
* [เพิ่มไฟล์เข้า Staging Area](#-เพิ่มไฟล์เข้า-staging-area)
* [Commit (บันทึกประวัติ)](#-commit-บันทึกประวัติ)
* [ดูประวัติ Commit](#️-ดูประวัติ-commit)
* [ย้อนการเปลี่ยนแปลง (Reset / Checkout)](#-ย้อนการเปลี่ยนแปลง-reset--checkout)
* [Hard / Soft / Mixed Reset คืออะไร](#-hard--soft--mixed-reset-คืออะไร)
* [Branch](#-branch-หัวใจของการทำงานเป็นทีม)
* [Merge Branch](#-merge-branch)
* [Git Conflict](#️-git-conflict)
* [Remote Repository](#-remote-repository)
* [Push / Pull / Fetch](#️-push--pull--fetch)
* [.gitignore](#-gitignore)
* [Stash](#-stash-ซ่อนงานชั่วคราว)
* [Best Practices](#-best-practices)
* [ข้อควรระวังใหญ่](#-ข้อควรระวังใหญ่)
* [Workflow สรุปภาพรวม](#-สรุปภาพรวม-workflow)

คู่มือนี้ออกแบบมาสำหรับผู้เริ่มต้นจนถึงระดับใช้งานจริงในการทำงานเป็นทีม อธิบายแนวคิด คำสั่ง ตัวอย่างสถานการณ์จริง ข้อควรระวัง และ Best Practices

---

## 🧠 Git คืออะไร

**Git** คือระบบควบคุมเวอร์ชัน (Version Control System – VCS)
ใช้สำหรับ:

* เก็บประวัติการแก้ไขโค้ด
* ย้อนกลับไปดูหรือกู้โค้ดเวอร์ชันเก่า
* ทำงานร่วมกันหลายคนโดยไม่ทับไฟล์กัน

Git เป็น **Distributed Version Control**
หมายความว่า ทุกคนมี repository เต็มชุดอยู่ในเครื่องตัวเอง

---

## 📂 โครงสร้างพื้นฐานของ Git

```
Working Directory  →  Staging Area  →  Repository
(โฟลเดอร์งาน)        (รอ commit)      (ประวัติถาวร)
```

* **Working Directory**: ไฟล์ที่เรากำลังแก้ไข
* **Staging Area**: ไฟล์ที่เลือกแล้วว่าจะบันทึก
* **Repository**: ประวัติ commit ทั้งหมด

---

## ⚙️ การตั้งค่า Git ครั้งแรก

### ตั้งชื่อและอีเมล (สำคัญมาก)

```bash
git config --global user.name "Your Name"
git config --global user.email "you@email.com"
```

ตรวจสอบค่า:

```bash
git config --list
```

📌 ใช้กรณีไหน?

* ทำครั้งแรกครั้งเดียวต่อเครื่อง
* ใช้ระบุตัวตนใน commit

⚠️ ข้อควรระวัง

* อีเมลควรตรงกับ GitHub/GitLab เพื่อให้ commit นับถูก

---

## 📁 เริ่มต้นโปรเจกต์ Git

### สร้าง Git ใหม่ในโฟลเดอร์

```bash
git init
```

📌 ใช้เมื่อ:

* สร้างโปรเจกต์ใหม่จากศูนย์
* ยังไม่เชื่อม remote

---

### Clone โปรเจกต์จาก Remote

```bash
git clone https://github.com/user/repo.git
```

📌 ใช้เมื่อ:

* ดึงโปรเจกต์ที่มีอยู่แล้วมาทำงาน

---

## 🔍 ตรวจสอบสถานะไฟล์

```bash
git status
```

แสดง:

* ไฟล์ที่แก้ไข
* ไฟล์ที่ยังไม่ stage
* ไฟล์ที่พร้อม commit

📌 ใช้บ่อยมาก ก่อน commit ทุกครั้ง

---

## ➕ เพิ่มไฟล์เข้า Staging Area

### เพิ่มไฟล์ทั้งหมด

```bash
git add .
```

### เพิ่มเฉพาะไฟล์

```bash
git add index.html
```

📌 ใช้เมื่อ:

* เลือกว่าไฟล์ไหนจะถูก commit

⚠️ ระวัง

* `git add .` จะเพิ่มทุกไฟล์ (รวมไฟล์ที่ไม่ตั้งใจ)

---

## 💾 Commit (บันทึกประวัติ)

```bash
git commit -m "เพิ่มฟังก์ชันคำนวณดอกเบี้ย"
```

📌 แนวคิดสำคัญ

* 1 commit = 1 งาน
* commit message ต้องอธิบายว่า **ทำอะไร**

❌ ไม่ควร

* commit หลายเรื่องในครั้งเดียว
* ใช้ข้อความว่า `fix`, `update`, `test`

✅ ตัวอย่างที่ดี

* `fix: calculate interest rounding`
* `feat: add customer loan summary`

---

## 🕰️ ดูประวัติ Commit

```bash
git log
```

แบบย่อ:

```bash
git log --oneline --graph --all
```

📌 ใช้เมื่อ:

* ตรวจสอบประวัติ
* หา commit ที่ต้องการย้อนกลับ

---

## 🔄 ย้อนการเปลี่ยนแปลง (Reset / Checkout)

### ยกเลิกการแก้ไขไฟล์ (ยังไม่ add)

```bash
git checkout -- file.txt
```

📌 ใช้เมื่อ:

* แก้ไฟล์พัง
* อยากกลับไปเวอร์ชันล่าสุดที่ commit

⚠️ ไฟล์จะ **หายทันที** ถ้ายังไม่ commit

---

### เอาไฟล์ออกจาก staging

```bash
git reset file.txt
```

📌 ใช้เมื่อ:

* add ผิดไฟล์
* ยังไม่อยาก commit ไฟล์นี้

---

### ย้อน commit (ยังไม่ push)

```bash
git reset --soft HEAD~1
```

📌 ใช้เมื่อ:

* commit ผิด
* อยากแก้ commit message หรือเพิ่มไฟล์

⚠️ ห้ามใช้ reset กับ commit ที่ push แล้ว (ในทีม)

---

## 🧨 Hard / Soft / Mixed Reset คืออะไร

Reset คือการ "ย้าย HEAD" กลับไปยัง commit ก่อนหน้า

> คำว่า **HEAD** = commit ปัจจุบันที่เรากำลังยืนอยู่

---

### 🔹 git reset --soft

```bash
git reset --soft HEAD~1
```

ผลลัพธ์:

* commit หาย
* โค้ดยังอยู่
* ยังอยู่ใน staging

📌 ใช้เมื่อ:

* commit message ผิด
* ลืม add ไฟล์

🧠 เปรียบเทียบ:

> "ลบแค่ประวัติ แต่เก็บงานไว้หมด"

---

### 🔸 git reset --mixed (ค่า default)

```bash
git reset HEAD~1
```

ผลลัพธ์:

* commit หาย
* โค้ดยังอยู่
* ออกจาก staging

📌 ใช้เมื่อ:

* อยากจัดไฟล์ใหม่ก่อน commit

🧠 เปรียบเทียบ:

> "ลบ commit + เอาไฟล์กลับมาแก้ใหม่"

---

### 🔥 git reset --hard (อันตราย)

```bash
git reset --hard HEAD~1
```

ผลลัพธ์:

* commit หาย
* โค้ดหาย
* ไม่เหลืออะไร

❌ ใช้เมื่อ:

* มั่นใจ 100% ว่าไม่ต้องการงานนั้น

🚨 คำเตือนใหญ่:

* ใช้ผิด = งานหายถาวร
* ห้ามใช้กับ commit ที่ push แล้ว

🧠 เปรียบเทียบ:

> "เผาทิ้งหมด ทั้งประวัติและงาน"

---

## 🌿 Branch (หัวใจของการทำงานเป็นทีม)

### ดู branch ทั้งหมด

```bash
git branch
```

### สร้าง branch ใหม่

```bash
git branch feature/login
```

### สลับ branch

```bash
git checkout feature/login
```

หรือ

```bash
git switch feature/login
```

📌 ใช้เมื่อ:

* แยกงานใหม่
* ทดลองโค้ด

❌ ไม่ควรทำงานบน main ตรงๆ

---

## 🔀 Merge Branch

```bash
git checkout main
git merge feature/login
```

📌 ใช้เมื่อ:

* งานเสร็จและต้องรวมเข้าหลัก

⚠️ ระวัง Conflict

---

## ⚔️ Git Conflict

เกิดเมื่อ:

* แก้ไฟล์เดียวกันคนละจุด

ขั้นตอนแก้:

1. เปิดไฟล์ที่ conflict
2. แก้โค้ดให้ถูก
3. git add
4. git commit

📌 Conflict เป็นเรื่องปกติ ไม่ใช่ความผิด

---

## 🌐 Remote Repository

### ดู remote

```bash
git remote -v
```

### เพิ่ม remote

```bash
git remote add origin https://github.com/user/repo.git
```

---

## ⬆️ Push (ส่งโค้ดขึ้น Remote)

```bash
git push origin main
```

ครั้งแรก:

```bash
git push -u origin feature/login
```

📌 ใช้เมื่อ:

* ต้องการแชร์งาน
* สำรองโค้ด

---

## ⬇️ Pull / Fetch

### ดึงและ merge

```bash
git pull
```

### ดึงอย่างเดียว

```bash
git fetch
```

📌 ใช้เมื่อ:

* อัปเดตโค้ดจากทีม

---

## 🧹 .gitignore

ใช้สำหรับบอก Git ว่า **ไม่ต้องติดตามไฟล์พวกนี้**

ตัวอย่าง:

```
node_modules/
.env
dist/
```

📌 ใช้เมื่อ:

* ไฟล์ build
* secret

⚠️ ไฟล์ที่ commit ไปแล้ว ignore ไม่ได้

---

## 🧪 Stash (ซ่อนงานชั่วคราว)

```bash
git stash
git stash pop
```

📌 ใช้เมื่อ:

* งานยังไม่เสร็จ
* ต้องสลับ branch ด่วน

---

## ✅ Best Practices

* commit เล็ก ๆ บ่อย ๆ
* ใช้ branch แยกงาน
* pull ก่อนเริ่มงานทุกครั้ง
* อย่า force push ถ้าไม่จำเป็น
* อ่าน commit ของตัวเองก่อน push

---

## 🚨 ข้อควรระวังใหญ่

❌ ห้าม

* push password / token
* reset --hard บน branch ทีม
* ทำงานบน main โดยตรง

---

## 🎯 สรุปภาพรวม Workflow

```
clone → branch → add → commit → push → merge
```

---

ถ้าต้องการ:

* Cheat Sheet 1 หน้า
* Git Flow สำหรับทีม
* ตัวอย่าง Workflow จริงในบริษัท

บอกได้เลยครับ 👌
